var data2;

$(document).ready(function(){

   /* $.get("tests/api_que.php", function(data, status){
        data2=JSON.parse(data);
        console.log(data2);
        for (index = 0; index < data2.length; ++index) {
            $('[name="currency"]').append($('<option>', {
                value: data2[index].price_usd,
                text: data2[index].symbol
            }));
        }
    });

    $.get("tests/fiat_api.php", function(data, status){
        data2=JSON.parse(data);
        $.each(data2.rates, function( key, value ) {

            $('[name="money"]').append($('<option>', {
                value: value,
                text: key
            }));
        });
    });*/
});